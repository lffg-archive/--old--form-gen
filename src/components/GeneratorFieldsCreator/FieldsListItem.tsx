import React from 'react'
import { Field } from '../../utils/fields/types'

export interface IProps {
  field: Field
}

export default function FieldsListItem(props: IProps) {
  return <div>{}</div>
}
